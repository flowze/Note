// Пример JavaScript для дополнительной интерактивности

// При загрузке страницы выводим сообщение в консоль
document.addEventListener('DOMContentLoaded', function() {
    console.log('Главная страница NoteApp загружена!');
});

// Можно добавить дополнительные функции по необходимости
// Скрипт для дополнительной интерактивности на странице заметок

document.addEventListener('DOMContentLoaded', function() {
    console.log('Страница заметок загружена!');
    // Здесь можно добавить дополнительную логику, например, обработку событий клика на карточках
});
